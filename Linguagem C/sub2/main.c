#include <stdio.h>
#include <stdlib.h>
lerDados(float x, float y)
{
    scanf("%f", &x);
    scanf("%f", &x);
}

int main()
{
    float x, y;
    x=0.00;
    y=2.34;
    lerDados(x, y);
    printf("x = %.2d e y = %.2f", x, y);
    return 0;
}
